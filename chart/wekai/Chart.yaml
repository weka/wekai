apiVersion: v2
name: wekai
description: Runs an embedded LLM traffic replay (wekai benchmark auto --router-replay-file) against a target model endpoint
type: application
version: 0.1.0
appVersion: "1.0.0"
